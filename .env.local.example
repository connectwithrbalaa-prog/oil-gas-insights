# Backend API base URL (used when the /backend proxy is unavailable)
VITE_API_BASE_URL=http://your-api-host:8001

# Set to "true" to force mock data even when the API is reachable
VITE_USE_MOCK_DATA=false
